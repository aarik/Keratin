# Ops utilities

- `diagnose_rhino_connection.py`: quick connectivity check to Rhino-side socket server.
- `log_manager.py`: view/filter logs across `./logs/`.

These are optional helpers and do not affect the core MCP server.
